# KanjiReader Environment Variables
# Copy this file to .env and fill in your values
#
# SECURITY NOTES:
# - Never commit .env to git (it's in .gitignore)
# - Each key should be restricted to its specific API in GCP Console
# - Consider adding app restrictions (Android package / iOS bundle ID)

# Google Cloud Vision API Key (required for OCR)
# 1. GCP Console → APIs & Services → Credentials → Create API Key
# 2. Restrict to: Cloud Vision API only
EXPO_PUBLIC_GOOGLE_CLOUD_API_KEY=your_vision_api_key_here

# Google Cloud Translation API Key (required for translations)
# 1. GCP Console → APIs & Services → Credentials → Create API Key
# 2. Restrict to: Cloud Translation API only
# Note: Using separate keys for security (principle of least privilege)
EXPO_PUBLIC_GOOGLE_TRANSLATE_API_KEY=your_translate_api_key_here

# Google Cloud TTS (optional, for premium voices - v2)
# GOOGLE_CLOUD_TTS_API_KEY=your_tts_api_key_here
